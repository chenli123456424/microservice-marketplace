server:
  port: 8081
  tomcat:
    # 增加HTTP请求头大小限制（单位：字节，50MB = 52428800字节）
    max-http-header-size: 52428800
    # 增加HTTP POST请求大小限制
    max-http-post-size: 52428800

spring:
  application:
    name: user-service # 服务名称
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    url: jdbc:mysql://localhost:3307/ecommerce_user?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.qq.com
    port: 587
    username: 1758006511@qq.com  # 替换为你的QQ邮箱
    password: whciopkjpjxxddaj  # 替换为你的QQ邮箱授权码
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      host: localhost
      port: 6379
  main:
    allow-circular-references: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
      enabled: true
  # 增加HTTP请求体和响应体大小限制
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

mybatis-plus:
    mapper-locations: classpath*:mapper/*.xml
    global-config:
        db-config:
            id-type: auto

app:
  jwt:
      secret: C#fB&v2@zL8qR9pW*sE$hN7gY!uM4jK3aG6dF5tH2bV1nJ0cZ_xL-kP+oI>uY<t

logging:
  level:
    org.springframework.security: DEBUG